<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="70dp"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="100dp"
    android:orientation="vertical"
    tools:context="com.example.flerchy.rk1.MainActivity">

    <TextView
        android:text="TextView"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/city_weather_view" />

    <Button
        android:text="Выберите город"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/choose_city_button"
        android:layout_centerHorizontal="true"
        android:onClick="chooseCity" />

    <Button
        android:text="Запустить в фоновом режиме"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/background_mode_on_button"
        android:layout_centerHorizontal="true"
        android:onClick="updateStart"/>

    <Button
        android:text="Отключить фоновый режим"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/background_mode_off_button"
        android:layout_centerHorizontal="true"
        android:onClick="updateStop"/>

</LinearLayout>
